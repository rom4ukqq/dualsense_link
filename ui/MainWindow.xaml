<Window
    x:Class="DualSenseLinkUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DualSenseLinkUI"
    Title="DualSense Link">
    <Grid Padding="16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="12">
            <TextBlock x:Name="ConnectionStateText" Text="Disconnected" FontSize="18" />
            <TextBlock x:Name="BatteryText" Text="Battery: --%" FontSize="18" />
        </StackPanel>

        <local:ControllerDashboard x:Name="Dashboard" Grid.Row="1" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8">
            <Button Content="Refresh" Click="OnRefreshClicked" />
            <Button Content="Live ON" Click="OnLiveOnClicked" />
            <Button Content="Live OFF" Click="OnLiveOffClicked" />
        </StackPanel>

        <StackPanel Grid.Row="3" Spacing="8">
            <TextBlock Text="Raw HID Input Stream" FontSize="16" />
            <TextBox x:Name="RawInputTextBox" AcceptsReturn="True" IsReadOnly="True" TextWrapping="Wrap" />
        </StackPanel>
    </Grid>
</Window>
