<Window
    x:Class="DualSenseLinkUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DualSenseLinkUI"
    Title="DualSense Link">
    <Grid Padding="16" RowSpacing="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="ServiceBridgeText" Text="Service: checking..." FontSize="16" />
        <TextBlock x:Name="HidHideStateText" Grid.Row="1" Text="HidHide: unknown" FontSize="14" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="12">
            <TextBlock x:Name="ConnectionStateText" Text="Disconnected" FontSize="18" />
            <TextBlock x:Name="BatteryText" Text="Battery: --%" FontSize="18" />
        </StackPanel>

        <local:ControllerDashboard x:Name="Dashboard" Grid.Row="3" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" Spacing="8">
            <Button Content="Refresh" Click="OnRefreshClicked" />
            <Button Content="Live ON" Click="OnLiveOnClicked" />
            <Button Content="Live OFF" Click="OnLiveOffClicked" />
            <Button Content="HidHide ON" Click="OnHidHideOnClicked" />
            <Button Content="HidHide OFF" Click="OnHidHideOffClicked" />
        </StackPanel>

        <StackPanel Grid.Row="5" Spacing="8">
            <TextBlock Text="Raw HID Input Stream" FontSize="16" />
            <TextBox x:Name="RawInputTextBox" AcceptsReturn="True" IsReadOnly="True" TextWrapping="Wrap" />
        </StackPanel>
    </Grid>
</Window>
